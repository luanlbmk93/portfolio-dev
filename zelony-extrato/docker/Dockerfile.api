FROM node:20-alpine
WORKDIR /app

COPY server/package.json ./
RUN npm install --omit=dev

COPY server/ ./
COPY dist/ ./dist/

ENV NODE_ENV=production
ENV PORT=3000
EXPOSE 3000
CMD ["node", "index.js"]
