pandas>=2.2.0
pdfplumber>=0.11.0
pydantic>=2.6.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Principal (quando disponível para o formato do banco)
# Obs: no PyPI (Windows) as versões disponíveis hoje vão até 0.0.8
bankstatementparser>=0.0.8

# OCR determinístico (fallback quando PDF é imagem/escaneado)
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0
PyMuPDF>=1.24.0

# OFX (formato estruturado, recomendado)
ofxparse>=0.21

