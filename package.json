{
  "name": "odevcwb-apps",
  "private": true,
  "scripts": {
    "install:disparador": "npm install --prefix disparador-gmail/client && npm install --prefix disparador-gmail/server",
    "build:portfolio": "npm run build --prefix \"portfolio pessoal\"",
    "build:separador": "cross-env VITE_BASE=/separadorpdf/ npm run build --prefix separador",
    "build:disparador-client": "npm run build --prefix disparador-gmail/client",
    "build": "npm run build:portfolio && npm run build:separador && npm run build:disparador-client",
    "start:disparador": "npm start --prefix disparador-gmail/server"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
